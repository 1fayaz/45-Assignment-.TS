interface macbook {
    chip: string
    ram: number
    storage : number
    size : number
    colour:string
}

let macbook = {
    chip : "M3 Pro" ,
    ram: 64,
    Storage: 512,
    size : 16,
    colour : "starlight"
}

console.log(macbook);
